export class Frog {
  constructor(x, y) {
    const frog = { x, y };
  }

  draw() { // na start / po tym jak żaba jest win / lose

  }

  move() {
    // żaba skacze co jedno pole, jeden button.down = 1 pole
  }

  win() {
    // żaba dotarła do domku
  }

  lose() {
    // kolizja / wpadnięcie do wody itp > żaba znika
    // time > 30s, śmierć
  }
}